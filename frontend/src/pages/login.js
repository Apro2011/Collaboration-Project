import React from 'react';
import {Login as LoginForm} from '../components/';

const Login = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default Login;